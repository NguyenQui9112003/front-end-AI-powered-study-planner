import { Body } from '../layouts/body'
import { Header } from '../layouts/header'

export const HomePage = () => {
    return (
        <>
            <Header />
            <Body />
        </>
    );
};